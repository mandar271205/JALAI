FROM python:3.12-slim-bookworm

WORKDIR /app
RUN pip install --no-cache-dir fastapi uvicorn

COPY docker/ml_stub_app.py /app/ml_stub_app.py

EXPOSE 8001
CMD ["uvicorn", "ml_stub_app:app", "--host", "0.0.0.0", "--port", "8001"]
